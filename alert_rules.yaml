groups:
- name: k8s-alerts
  rules:
  - alert: PodFailure
    expr: kube_pod_status_phase{phase="Failed"} > 0
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "Pod failure detected"
